/**
 * Configuración global de Jest
 * Se ejecuta antes de cada test suite
 */

// Timeout global para tests asincrónicos
jest.setTimeout(10000);

// Setup adicional si es necesario (ej: limpiar mocks globales, configurar variables de entorno)
